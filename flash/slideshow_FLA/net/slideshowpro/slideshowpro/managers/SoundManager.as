
// frame 1 (DOINITACTION)
if(!_global.net)
{
	_global.net = new Object();

} // end if
;
if(!_global.net.slideshowpro)
{
	_global.net.slideshowpro = new Object();

} // end if
;
if(!_global.net.slideshowpro.slideshowpro)
{
	_global.net.slideshowpro.slideshowpro = new Object();

} // end if
;
if(!_global.net.slideshowpro.slideshowpro.managers)
{
	_global.net.slideshowpro.slideshowpro.managers = new Object();

} // end if
;
		if(!_global.net.slideshowpro.slideshowpro.managers.SoundManager)
{
	net.slideshowpro.slideshowpro.managers.SoundManager = "function(	)
	{
		this.sounds = new Array();
	}";
	function(	)
	{
		this.sounds = new Array();
	}.prototype;
depth)
		{
			this.startDepth = "depth";

		} // end if
			if(	function(	)
	{
		this.sounds = new Array();
	}.prototype.setContainer = "function(	mc,depth)
	{
		this.container = .createEmptyMovieClip(this.mcName,this.startDepth);
	}";
)
		{
			if(linkage == this.sounds.0.1)
			{

			} // end if
		while(			0++;
		} // end while
		if(.slice(0,7) == "file://")
		{
			new Sound(_local6).loadSound(.slice(7,.length));

		} // end if
		else
		{
			new Sound(_local6).attachSound();

		}
			if(	depth.addSound = "function(	linkage,name)
	{
		0;
		var _local6 = this.container.createEmptyMovieClip(this.mcName + this.soundCount,this.soundCount);
		new Sound(_local6);
		this.sounds.push(new Array(new Sound(_local6),linkage,_local6));
		this.soundCount = this.soundCount++;
	}";
)
		{
			if(this.sounds.0.1 == linkage)
			{
				delete this.sounds.0.0				removeSound;
				this.sounds.0.2.removeMovieClip();
				this.sounds.splice(0,1);

			} // end if
		while(			0++;
		} // end while
				if(	.(0++) = "function(	name)
	{
		0;
	}";
)
		{
this.sounds.0.1 == new Sound(_local6)&&			if(name > 0)
			{
				if(!name)
				{

				} // end if
				else
				{

				}
							this.sounds.0.0.setVolume(name);
				this.sounds.0.0.start();

			} // end if
		while(			0++;
		} // end while
	playSound.100 = "function(	name,volume)
	{
		0;
	}";
	(0++).mcName = "SoundManagerAsset";
	(0++).startDepth = 13333;
	(0++).soundCount = 0;

} // end if
ASSetPropFlags(net.slideshowpro.slideshowpro.managers.SoundManager.prototype,null,1);
