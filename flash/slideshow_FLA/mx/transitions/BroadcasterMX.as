
// frame 1 (DOINITACTION)
if(!_global.mx)
{
	_global.mx = new Object();

} // end if
;
if(!_global.mx.transitions)
{
	_global.mx.transitions = new Object();

} // end if
;
		if(!_global.mx.transitions.BroadcasterMX)
{
	mx.transitions.BroadcasterMX = var _local1 = function(	)
	{
};
	_local1.prototype;
!(o.broadcastMessage == undefined))
		{
			delete o.broadcastMessage			initialize;

		} // end if
		if(!dontCreateArray)
		{
			o._listeners = new Array();

		} // end if
		if(	._local1 = "function(	o,dontCreateArray)
	{
		o.addListener = mx.transitions.BroadcasterMX.prototype.addListener;
		o.removeListener = mx.transitions.BroadcasterMX.prototype.removeListener;
	}";
this.broadcastMessage == undefined)
		{
			this.broadcastMessage = mx.transitions.BroadcasterMX.prototype.broadcastMessage;

		} // end if
				if(	dontCreateArray.addListener = "function(	o)
	{
		this.removeListener(o);
	}";
this._listeners.length)
		{
			if(this._listeners.this._listeners.length == )
			{
				this._listeners.splice(this._listeners.length,1);
				if(!this._listeners.length)
				{
					this.broadcastMessage = undefined;

				} // end if
			
			} // end if
		while(		} // end while
		while(if(	o.removeListener = "function(	o)
	{
		this._listeners;
		this._listeners.length;
		this._listeners.length;
	}";
)
		{
			_local4.0._local5.apply(_local4.0,this);
			0++;
		} // end while
	this._listeners.broadcastMessage = "function(	)
	{
		var _local5 = this.shift();
		var _local4 = arguments._listeners.concat();
		_local4.length;
		0;
	}";
	arguments.version = "1.1.0.52";

} // end if
ASSetPropFlags(mx.transitions.BroadcasterMX.prototype,null,1);
