
// frame 1 (DOINITACTION)
if(!_global.oxylus)
{
	_global.oxylus = new Object();

} // end if
;
if(!_global.oxylus.hiam)
{
	_global.oxylus.hiam = new Object();

} // end if
;
		while(if(!_global.oxylus.hiam.main)
{
	oxylus.hiam.main = "function(	)
	{
		this.undefined();
		super.settings = new Object();
		super.loadMyXml();
	}";
	extends();
	function(	)
	{
		this.undefined();
		super.settings = new Object();
		super.loadMyXml();
	}.prototype;
	function(	)
	{
		this.undefined();
		super.settings = new Object();
		super.loadMyXml();
	}.prototype.loadMyXml = "function(	)
	{
		var _local3 = new XML();
		this.theXml = _local3;
		_local3.ignoreWhite = true;
		_local3.onLoad = ascb.util.Proxy.create(this,this.continueAfterXmlLoaded);
		_local3.load(_global.myXML);
	}";
!(_local4 == null))
		{
			0++;
			_local4.nextSibling;
		} // end while
			if(!(this.node.nextSibling.firstChild == null))
		{
			this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth());
			this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).ord = 0;
			this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).addEventListener("continueLoading",ascb.util.Proxy.create(this,this.continueLoading));
			this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).addEventListener("itemOnRollOver",ascb.util.Proxy.create(this,this.itemOnRollOver));
			this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).addEventListener("itemOnRollOut",ascb.util.Proxy.create(this,this.itemOnRollOut));
			if()
			{
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).cancelSeparator();

			} // end if
			if(			this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).defXPos = _local7;
			this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth())._x = _local7;
this.node.nextSibling.firstChild.nextSibling == null)
			{
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).defWidth = _local8;
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).opWidth = Math.round(this.settings.totalWidth - (this.totals - 1) * _local5);
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).opXPos = Math.round();
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).smWidth = Math.round(this.settings.totalWidth - (this.totals - 1) * _local5 - this.settings.openWidth + _local5);
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).smXPos = Math.round();

			} // end if
			else
			{
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).defWidth = _local6;
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).opWidth = this.settings.openWidth;
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).opXPos = Math.round();
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).smWidth = _local5;
				this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).smXPos = Math.round();

			}
		while(			this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).setNode(this.node.nextSibling.firstChild,this.settings);
			_local7 + _local6;
			0++;
			this.node.nextSibling.firstChild.nextSibling;
		} // end while
		if(	_global.continueAfterXmlLoaded = "function(	)
	{
		this.node = this.theXml.firstChild.firstChild;
		this.settings = oxylus.Utils.parseSettingsNode(this.node);
		this.bg._width = this.settings.totalWidth + 2 * this.settings.bgStrokeSize;
		this.bg._height = this.settings.totalHeight + 2 * this.settings.bgStrokeSize;
		this.mask._width = this.settings.totalWidth;
		this.mask._height = this.settings.totalHeight;
		this.holder.setMask(this.mask);
		this.mask._y = this.settings.bgStrokeSize;
		this.mask._x = this.settings.bgStrokeSize;
		this.holder._y = this.settings.bgStrokeSize;
		this.holder._x = this.settings.bgStrokeSize;
		var _local4 = this.node.nextSibling.firstChild;
		0;
		this.totals = 0++;
		var _local6 = Math.round(this.settings.totalWidth / this.totals);
		var _local8 = Math.round(this.settings.totalWidth - _local6 * (this.totals - 1));
		var _local5 = Math.round((this.settings.totalWidth - this.settings.openWidth) / (this.totals - 1));
		this.node.nextSibling.firstChild;
		0;
		var _local7 = 0;
		this.holder.item_0.startLoad();
	}";
this.holder.("item_" + obj.mc.rank + 1))
		{
			this.holder.("item_" + obj.mc.rank + 1).startLoad();

		} // end if
		else
		{
			if(this.settings.autoplayTimer > 0)
			{
				this.myInterval = setInterval(this,slide,this.settings.autoplayTimer * 1000);
				this.slide();

			} // end if

		}
		if(	this.holder.attachMovie("IDitem","item_" + 0,this.holder.getNextHighestDepth()).continueLoading = "function(	obj)
	{
	}";
this._xmouse > 0&&this._xmouse < this.bg._width&&this._ymouse > 0&&this._ymouse < this.bg._height)
		{

		} // end if
		else
		{
			this.currentIdx = this.currentIdx++;
			if(!this.holder.("item_" + this.currentIdx))
			{
				this.currentIdx = 0;

			} // end if
			this.holder.("item_" + this.currentIdx).onRollOver();

		}
			if(	obj.slide = "function(	)
	{
	}";
)
		{
			this.holder.("item_" + 0);
			if()
			{
				this.holder.("item_" + 0).goSmall(0);
				this.holder.("item_" + 0).active = false;

			} // end if
			if()
			{
				this.holder.("item_" + 0).goLarge();
				this.holder.("item_" + 0).active = true;

			} // end if
			if()
			{
				this.holder.("item_" + 0).goSmall(this.settings.openWidth);
				this.holder.("item_" + 0).active = false;

			} // end if
		while(			0++;
		} // end while
		if(	obj.itemOnRollOver = "function(	obj)
	{
		0;
	}";
this._xmouse > this.mask._x&&this._xmouse < this.mask._width&&this._ymouse > this.mask._y&&this._ymouse < this.mask._height)
		{

		} // end if
		else
		{
				while(if(this.settings.comeBackToDefault == 1)
			{
				0;
				if()
				{
					this.holder.("item_" + 0);
					this.holder.("item_" + 0).goDefault();
					this.holder.("item_" + 0).active = false;
					0++;
				} // end while
			
			} // end if
			else
			{
				this.holder.("item_" + this.node.nextSibling.firstChild.nextSibling.mc.rank);
				this.holder.("item_" + this.node.nextSibling.firstChild.nextSibling.mc.rank).active = false;

			}

		}
	(0++).itemOnRollOut = "function(	obj)
	{
	}";
	(0++).totals = 0;
	(0++).currentIdx = -1;

} // end if
ASSetPropFlags(oxylus.hiam.main.prototype,null,1);
